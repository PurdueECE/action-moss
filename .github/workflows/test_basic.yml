name: Basic Test
on: push
jobs:
  test_basic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          user_id: ${{ secrets.MOSS_USER_ID }}
          args: -l python test_data/basic/main.py